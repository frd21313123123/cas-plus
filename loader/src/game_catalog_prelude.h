#pragma once

// Explicit standard declarations used by game_catalog.h.  The secure loader
// compiles a generated main.secure.cpp from the intermediate directory, so do
// not rely on transitive MSVC headers that can change between runner images.
#include <cstdlib>
#include <cwchar>
#include <cwctype>
#include <iterator>
